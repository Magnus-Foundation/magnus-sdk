node_modules
# dist/ is committed so consumers can install from a git URL without running the build
# (e.g. github:Magnus-Foundation/magnus-sdk#<sha> in package.json)
*.tsbuildinfo
.turbo
coverage
.gstack/
